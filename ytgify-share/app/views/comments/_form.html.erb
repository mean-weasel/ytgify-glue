<%= form_with model: [gif, comment || gif.comments.build],
              url: gif_comments_path(gif),
              data: { turbo_frame: "_top" },
              class: "mb-8" do |f| %>
  <div class="flex space-x-3">
    <!-- User Avatar -->
    <div class="w-10 h-10 bg-gradient-to-r from-[#E91E8C] to-[#7B2FBE] text-white rounded-full flex items-center justify-center font-semibold flex-shrink-0">
      <%= current_user.username.first.upcase %>
    </div>

    <div class="flex-1">
      <%= f.text_area :content,
          placeholder: "Add a comment...",
          rows: 3,
          autofocus: comment.present?,
          class: "w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg text-white placeholder-gray-500 focus:ring-2 focus:ring-[#E91E8C] focus:border-transparent resize-none transition-shadow #{'border-red-500' if comment&.errors&.any?}" %>

      <!-- Error Messages -->
      <% if comment&.errors&.any? %>
        <div class="mt-2 p-3 bg-red-900/30 border border-red-800 rounded-lg">
          <div class="flex items-start">
            <svg class="w-5 h-5 text-red-400 mr-2 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
            </svg>
            <div class="text-sm text-red-400">
              <%= comment.errors.full_messages.to_sentence %>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Submit Button -->
      <div class="mt-3 flex justify-end space-x-2">
        <%= f.submit "Post Comment",
            class: "px-6 py-2 bg-gradient-to-r from-[#E91E8C] to-[#7B2FBE] text-white rounded-lg hover:opacity-90 font-medium cursor-pointer transition-colors",
            data: { turbo_submits_with: "Posting..." } %>
      </div>
    </div>
  </div>
<% end %>
