<%
  # Determine if current user is following this user
  is_following = user_signed_in? && current_user.following?(user)
  error_message = local_assigns[:error]
%>

<%= turbo_frame_tag "follow_button_#{user.id}", class: "inline-block" do %>
  <% if user_signed_in? %>
    <% if current_user == user %>
      <!-- Don't show follow button for own profile -->
    <% else %>
      <%= button_to follow_user_path(user.username),
          method: :post,
          data: {
            controller: "follow",
            follow_user_id_value: user.id,
            follow_username_value: user.username,
            turbo_frame: "follow_button_#{user.id}"
          },
          class: "group relative px-4 py-2 min-h-[44px] rounded-lg font-medium transition-all duration-200 #{is_following ? 'bg-gray-800 text-gray-300 hover:bg-red-900/30 hover:text-red-400' : 'bg-gradient-to-r from-[#E91E8C] to-[#7B2FBE] text-white hover:opacity-90'}" do %>

        <span class="follow-button-text">
          <% if is_following %>
            <span class="group-hover:hidden">Following</span>
            <span class="hidden group-hover:inline">Unfollow</span>
          <% else %>
            Follow
          <% end %>
        </span>

        <!-- Loading spinner (hidden by default) -->
        <span class="follow-button-loading hidden">
          <svg class="animate-spin h-5 w-5 inline-block" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
        </span>
      <% end %>

      <% if error_message.present? %>
        <div class="absolute top-full left-0 mt-2 px-3 py-2 bg-red-900/50 border border-red-800 rounded-lg text-sm text-red-400 whitespace-nowrap">
          <%= error_message %>
        </div>
      <% end %>
    <% end %>
  <% else %>
    <!-- Show sign-in prompt for non-authenticated users -->
    <%= link_to "Follow",
        new_user_session_path,
        class: "px-4 py-2 min-h-[44px] inline-flex items-center bg-gradient-to-r from-[#E91E8C] to-[#7B2FBE] text-white hover:opacity-90 rounded-lg font-medium transition-colors" %>
  <% end %>
<% end %>
