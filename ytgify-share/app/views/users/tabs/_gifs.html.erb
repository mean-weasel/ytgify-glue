<% if gifs.any? %>
  <!-- GIFs Grid -->
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
    <%= render partial: "gifs/gif_card", collection: gifs, as: :gif %>
  </div>

  <!-- Pagination -->
  <% if pagy.pages > 1 %>
    <div class="flex justify-center items-center space-x-2">
      <%== pagy_nav(pagy, link_extra: 'data-turbo-frame="profile_content" class="px-4 py-2 border border-gray-300 rounded-md hover:bg-gray-50 transition-colors text-gray-700"') %>
    </div>
  <% end %>
<% else %>
  <!-- Empty State -->
  <div class="text-center py-16">
    <svg class="mx-auto h-16 w-16 text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 4v16M17 4v16M3 8h4m10 0h4M3 12h18M3 16h4m10 0h4M4 20h16a1 1 0 001-1V5a1 1 0 00-1-1H4a1 1 0 00-1 1v14a1 1 0 001 1z" />
    </svg>

    <h3 class="text-xl font-semibold text-gray-700 mb-2">No GIFs yet</h3>

    <p class="text-gray-600 mb-6">
      <% if user_signed_in? && current_user == user %>
        You haven't uploaded any GIFs yet. Start creating!
      <% else %>
        <%= user.display_name || user.username %> hasn't uploaded any GIFs yet.
      <% end %>
    </p>

    <% if user_signed_in? && current_user == user %>
      <%= link_to new_app_gif_path,
          class: "inline-block px-6 py-3 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 font-medium transition-colors" do %>
        Upload Your First GIF
      <% end %>
    <% end %>
  </div>
<% end %>
